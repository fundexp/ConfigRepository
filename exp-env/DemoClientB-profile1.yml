---
spring:
  profiles: profile1
  rabbitmq:
    listener:
      auto-startup: true
    host: localhost
    port: 5672
    username: ihsanever
    password: guming0927
    publisher-confirms: true
  cloud:
    config:
      server:
        monitor:
          github:
            enable: true
      discovery:
        enabled: true

eureka:
  instance:
    prefer-ip-address: true
    ip-address: ${SERVER_ADDRESS}
    metadataMap:
      instanceId: ${spring.application.name}:${random.value}}
  client:
    serviceUrl:
      defaultZone: http://localhost:18801/eureka/

server:
  port: ${SERVER_PORT}

endpoints:
  restart:
    enable: true

content: o my ladygaga
---