---
spring:
  profiles: expeureka02

server:
  port: ${SERVER_PORT}

eureka:
  instance:
    prefer-ip-address: true
    ip-address: ${HOST_ADDRESS}
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_SERVERS}
